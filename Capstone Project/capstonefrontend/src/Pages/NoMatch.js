// Page to display if the user tries to directly access a URL that is not valid
const NoMatch = () => {
  return (
    <div>
      <h1>NO PAGE FOUND!</h1>
    </div>
  );
};

export default NoMatch;
